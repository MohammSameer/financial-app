# Copy to .env.local for local development.
#
# Base URL of the FastAPI backend, with no trailing slash. NEXT_PUBLIC_ is
# required for the value to reach the browser, which is where the fetches
# happen — see DECISIONS.md on why the dashboard is client-rendered.
NEXT_PUBLIC_API_URL=http://127.0.0.1:8000
